name: compute
title: Compute Job Scaffold
description: Generate reproducible compute jobs and runtime outputs for a task.
keywords:
  - compute
  - simulation
  - reproducibility
  - hashes
outputs:
  - review: "AGENTS/tasks/<task_id>/review/compute_skill_report.md"
  - patch: "AGENTS/tasks/<task_id>/deliverable/patchset/patch.diff"
requires_network: false
preferred_runner:
  - codex
risk: medium
confirmations:
  - Running compute jobs may consume CPU and create outputs under AGENTS/tasks.
