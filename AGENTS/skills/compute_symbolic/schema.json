{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "compute_symbolic result",
  "type": "object",
  "required": ["meta", "params", "results", "sanity_checks", "diagnostics"],
  "properties": {
    "meta": {
      "type": "object",
      "required": ["task_id", "skill", "backend", "timestamp_utc", "status"]
    },
    "params": {"type": "object"},
    "results": {"type": "object"},
    "sanity_checks": {"type": "array"},
    "diagnostics": {"type": "object"}
  }
}
