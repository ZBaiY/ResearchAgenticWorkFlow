#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
ROOT="$SCRIPT_DIR"
TARGET="$ROOT/bin/a"

if [ ! -x "$TARGET" ]; then
  echo "ERROR: missing executable $TARGET" >&2
  exit 1
fi

exec "$TARGET" "$@"
